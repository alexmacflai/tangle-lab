# @tangle-lab/shared

Small shared package for code that is reused by more than one prototype.

Keep experimental work inside the prototype that needs it first. Move code here only when reuse is real, the API is clear, and the shared version makes future prototypes easier to maintain.
